node_modules/
dist/
release/
ms-playwright/
sandbox/results/

# Hermes planning workspace (local working docs, not part of the app)
.hermes/

# Claude Code local tooling (dev-server preview config, etc.)
.claude/

# Windows "Zone.Identifier" files (NTFS alternate data streams from Internet
# downloads). Pattern has no colon so it also catches the U+F03A look-alike
# variant a cross-platform checkout can produce.
*Zone.Identifier
